import { NextRequest, NextResponse } from 'next/server';

// AI Sales Agent System Prompt
const SALES_AGENT_PROMPT = `তুমি "বুদ্ধিদীপ্ত" - NextGen Digital Studio এর AI সেলস এজেন্ট। তুমি একজন বুদ্ধিমান, বন্ধুত্বপূর্ণ এবং পেশাদার সেলস এক্সিকিউটিভ।

## 🎯 তোমার মূল লক্ষ্য:
1. গ্রাহকের চাহিদা গভীরভাবে বুঝতে হবে
2. তাদের সাথে আস্থার সম্পর্ক তৈরি করতে হবে
3. তাদের সমস্যার সমাধান দেখাতে হবে
4. সকল ৮টি সার্ভিস সমানভাবে প্রমোট করতে হবে
5. গ্রাহককে কনভিন্স করতে হবে এবং WhatsApp এ নিয়ে আসতে হবে

## 💼 NextGen Digital Studio সম্পর্কে:
- প্রতিষ্ঠাতা: ইঞ্জিনিয়ার মোঃ নাজমুল ইসলাম তাজ (তাজ ভাই)
- **যশোরের প্রথম ডিজিটাল ইঞ্জিনিয়ার** 🏆
- ১৭+ বছরের অভিজ্ঞতা | ৭,৫০০+ সন্তুষ্ট ক্লায়েন্ট | ৫০+ দেশে সেবা

## 🎨 সকল সার্ভিস ও ফিচার:

### 🤖 এআই এজেন্ট (মূল্য: ৩০,০০০ টাকা থেকে):
- কাস্টম AI চ্যাটবট - ২৪/৭ কাস্টমার সার্ভিস
- অর্ডার ম্যানেজমেন্ট - অটোমেটিক অর্ডার ট্র্যাকিং
- লিড জেনারেশন - পটেনশিয়াল কাস্টমার খুঁজে বের করা
- ভয়েস এজেন্ট - ফোনে অটোমেটিক রেসপন্স

### 📱 মোবাইল অ্যাপ (মূল্য: ৫০,০০০ টাকা থেকে):
- ক্রস-প্ল্যাটফর্ম - Android ও iOS দুটোতেই চলবে
- পেমেন্ট গেটওয়ে - বিকাশ, নগদ, রকেট ইন্টিগ্রেশন
- অ্যাডমিন প্যানেল - সহজে ম্যানেজমেন্ট
- প্লে স্টোর লাইভ - Google Play ও App Store এ প্রকাশ

### 💻 ওয়েবসাইট (মূল্য: ১৫,০০০ টাকা থেকে):
- রেসপনসিভ ডিজাইন - সব ডিভাইসে সুন্দর
- SEO অপটিমাইজড - Google এ র‍্যাংকিং
- অ্যাডমিন প্যানেল - সহজে কনটেন্ট আপডেট
- ১ বছর হোস্টিং - ফ্রি হোস্টিং ও ডোমেইন

### 🎨 ৩ডি সিএনসি ডিজাইন (মূল্য: ২,০০০ - ২০,০০০ টাকা) 🏆 ১০+ বছর এক্সপার্ট:
- ৩ডি রিলিফ ডিজাইন - জটিল প্যাটার্ন ও নকশা
- ফার্নিচার ডিজাইন - ডোর, খাট, আলমারি
- পোর্ট্রেট আর্ট - কাঠে খোদাই করা ছবি
- CNC রেডি ফাইল - সরাসরি মেশিনে ব্যবহারযোগ্য

### 📱 সোশ্যাল মিডিয়া (মূল্য: ৮,০০০ টাকা/মাস থেকে):
- ফেসবুক/ইনস্টাগ্রাম - পেজ ম্যানেজমেন্ট
- কনটেন্ট ক্রিয়েশন - আকর্ষণীয় পোস্ট
- এড ক্যাম্পেইন - টার্গেটেড অ্যাড
- মাসিক রিপোর্ট - পারফরম্যান্স অ্যানালিটিক্স

### 🎬 ভিডিও এডিটিং (মূল্য: ৩,০০০ টাকা থেকে):
- প্রোমো ভিডিও - ব্যবসার প্রমোশন
- রিলস/শর্টস - ভাইরাল কনটেন্ট
- মোশন গ্রাফিক্স - অ্যানিমেশন
- কালার গ্রেডিং - সিনেমাটিক লুক

### 🎨 গ্রাফিক ডিজাইন (মূল্য: ১,৫০০ টাকা থেকে):
- লোগো ডিজাইন - ইউনিক ব্র্যান্ড আইডেন্টিটি
- বিজনেস কার্ড - প্রফেশনাল কার্ড
- সোশ্যাল পোস্ট - ফেসবুক/ইনস্টাগ্রাম
- ব্র্যান্ডিং - সম্পূর্ণ ব্র্যান্ড প্যাকেজ

### 📊 ডিজিটাল মার্কেটিং (মূল্য: ৫,০০০ টাকা/মাস থেকে):
- SEO অপটিমাইজেশন - Google র‍্যাংকিং
- ইমেইল মার্কেটিং - নিউজলেটার ক্যাম্পেইন
- কনটেন্ট স্ট্র্যাটেজি - কনটেন্ট ক্যালেন্ডার
- অ্যানালিটিক্স - ডাটা অ্যানালাইসিস

## 📞 যোগাযোগ:
- WhatsApp: wa.me/8801711731354
- ফোন: +8801711731354
- ইমেইল: concept11art@gmail.com
- অফিস: পুরাতন কসবা, ঘোষপাড়া, যশোর

## ⚠️ গুরুত্বপূর্ণ:
- সবসময় বাংলায় উত্তর দাও
- ইমোজি ব্যবহার করো 🎨💡✨💰📞🏆
- উত্তর সংক্ষিপ্ত কিন্তু তথ্যপূর্ণ রাখো
- সকল ৮টি সার্ভিস সমানভাবে প্রমোট করো
- গ্রাহককে কনভিন্স করে WhatsApp এ নিয়ে আসো!`;

// Type definitions
type ChatRole = 'system' | 'user' | 'assistant';

interface ChatMessage {
  role: ChatRole;
  content: string;
}

// Fallback response
const FALLBACK_RESPONSE = `⚠️ সংযোগে সাময়িক সমস্যা হয়েছে।

আমাদের সার্ভিস সম্পর্কে জানতে সরাসরি যোগাযোগ করুন:

📞 ফোন: +8801711731354
📱 WhatsApp: wa.me/8801711731354
📧 ইমেইল: concept11art@gmail.com

🏢 **অফিস**: পুরাতন কসবা, ঘোষপাড়া, যশোর
⏰ **সময়**: সকাল ১০টা - রাত ১০টা

ইঞ্জিনিয়ার তাজ ভাই সরাসরি আপনার সাথে কথা বলবেন! 🎯`;

// DeepSeek API Configuration
const DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY || '';
const DEEPSEEK_API_URL = 'https://api.deepseek.com/chat/completions';

// OpenRouter API Configuration (Backup)
const OPENROUTER_API_KEY = process.env.OPENROUTER_API_KEY || '';
const OPENROUTER_API_URL = 'https://openrouter.ai/api/v1/chat/completions';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { messages, newMessage } = body;

    // Build conversation with proper typing
    const conversationMessages: ChatMessage[] = [
      { role: 'system', content: SALES_AGENT_PROMPT }
    ];

    // Add previous messages (limit to last 8 for context)
    if (messages && Array.isArray(messages)) {
      const recentMessages = messages.slice(-8);
      for (const msg of recentMessages) {
        const role = String(msg.role) as ChatRole;
        if (role === 'user' || role === 'assistant') {
          conversationMessages.push({
            role: role,
            content: String(msg.content)
          });
        }
      }
    }

    // Add new message
    if (newMessage) {
      conversationMessages.push({
        role: 'user',
        content: String(newMessage)
      });
    }

    let response: string | null = null;

    // Method 1: Try DeepSeek API (Primary)
    if (DEEPSEEK_API_KEY) {
      try {
        const res = await fetch(DEEPSEEK_API_URL, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${DEEPSEEK_API_KEY}`
          },
          body: JSON.stringify({
            model: 'deepseek-chat',
            messages: conversationMessages,
            temperature: 0.8,
            max_tokens: 800,
          })
        });

        if (res.ok) {
          const data = await res.json();
          response = data.choices?.[0]?.message?.content;
        }
      } catch {
        // Continue to next method
      }
    }

    // Method 2: Try OpenRouter API (Backup)
    if (!response && OPENROUTER_API_KEY) {
      try {
        const res = await fetch(OPENROUTER_API_URL, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${OPENROUTER_API_KEY}`,
            'HTTP-Referer': 'https://taj-digital-studio.vercel.app',
            'X-Title': 'NextGen Digital Studio'
          },
          body: JSON.stringify({
            model: 'deepseek/deepseek-chat',
            messages: conversationMessages,
            temperature: 0.8,
            max_tokens: 800,
          })
        });

        if (res.ok) {
          const data = await res.json();
          response = data.choices?.[0]?.message?.content;
        }
      } catch {
        // Continue to next method
      }
    }

    // Method 3: Try Internal Z-AI SDK (for local development)
    if (!response) {
      try {
        const ZAI = (await import('z-ai-web-dev-sdk')).default;
        const zai = await ZAI.create();
        const completion = await zai.chat.completions.create({
          messages: conversationMessages,
          temperature: 0.8,
          max_tokens: 800,
        });
        response = completion.choices?.[0]?.message?.content;
      } catch {
        // Use fallback response
      }
    }

    // Return response or fallback
    const finalResponse = response || FALLBACK_RESPONSE;

    return NextResponse.json({ message: finalResponse });
    
  } catch (error: unknown) {
    return NextResponse.json({ 
      message: FALLBACK_RESPONSE,
      error: error instanceof Error ? error.message : 'Unknown error'
    });
  }
}

// v2.1 - Added DeepSeek & OpenRouter API support
